# 非遗影像工坊 2.1：让 AI 当你的非遗短片导演

## 一个痛点

你想用 AI 做一支非遗短片。你打开了 Seedream，准备生成画面——然后发现，你不知道写什么提示词。你不知道第一个镜头该拍什么，不知道角色该穿什么衣服，不知道怎么把"蜀绣"这个主题拆成 8 个能用的镜头。你也不知道参赛说明该怎么写。

你卡在了"创意"这一步，而不是"生成"这一步。

## 这个工具解决什么

**非遗影像工坊** 是一个 AI 辅助的非遗短片创作台。你只需要做三件事：

1. 选一个非遗类型（蜀绣、傩戏、铜梁龙舞、木版年画……）
2. 写一句创意主题
3. 选一个视觉风格

然后 AI 会用 30 到 90 秒，给你一份完整的短片方案：创意故事、角色设定、场景设定、8 个分镜镜头（每个带首帧/尾帧/视频提示词）、声音设计、文化表达检查、参赛说明、发布文案。

拿到方案后，你可以直接把提示词复制到 Seedream 或 Seedance 里生成画面。也可以对任意一个镜头单独重新生成、单独优化提示词，直到满意为止。

## 真实 AI，不是演示数据

V2.1.0 接入了 DeepSeek Chat 大模型。所有生成内容都是 AI 实时产生的，不是预设的 mock 数据。

你可以：
- 对故事模块单独重新生成，角色和场景保持不变
- 对第 3 个镜头重新生成，AI 会自动考虑第 2 个和第 4 个镜头的衔接
- 对某个镜头的提示词做"增强电影感"优化
- 对视频提示词做"适配 Seedance"优化
- 对首帧提示词做"简化"优化

9 种优化方向，覆盖了从创意表达到工程落地的完整链路。

## 技术架构

前端 React 18 + TypeScript + Vite 6，后端 Express 4 + TypeScript。AI 请求通过后端代理，API Key 存在于服务端 .env 文件，前端零泄露。

后端有一套 JSON 修复链，处理 AI 返回的不规范格式（代码围栏、截断、多余文字）。还有数据标准化层，强制镜头补齐到 8 个，缺失字段自动填充。可生成性评分基于 8 个维度启发式打分，帮用户预判提示词的生成难度。

## 两种体验方式

**快速体验**：下载 HTML 压缩包，双击打开，选"快速体验"。无需 API Key，数秒生成完整方案。适合快速了解产品功能。

**AI 真实生成**：下载源码，配置 .env 文件填入 DeepSeek API Key，运行 `npm run dev:all`。所有内容由 AI 实时生成，可反复重新生成和优化。

## 用 TRAE 怎么开发的

全程使用 TRAE。从前端骨架生成到后端 API 开发，从复杂交互逻辑到全站回归测试，TRAE 贯穿了整个开发流程。

最关键的几个场景：

1. **AI 导演台长页面**：sticky 导航 + IntersectionObserver 滚动高亮 + 提示词展开收起 + 编辑状态管理，这些复杂交互的逻辑都是在 TRAE 协助下逐段完成的。

2. **后端 AI 代理**：6 个 API 接口、JSON 修复链、数据标准化、内容安全规则、可生成性评分，全部通过 TRAE 生成和验证。

3. **真实 AI 测试**：接入 DeepSeek 后，在 TRAE 中直接测试了全部 5 种 AI 操作（整体生成、单模块重新生成、单镜头重新生成、镜头优化、提示词优化），全部通过。

4. **安全验证**：用 TRAE 检查了前端代码、dist 构建产物、Git 历史中是否泄露 API Key，确认零泄露。

## 4 个内置案例

如果你不想自己生成，可以直接打开案例库：

- **傩戏《丑面》**：贵州德江傩堂戏，面具背后的信仰与人性
- **铜梁龙《火龙入夜》**：重庆铜梁火龙，铁水与勇气的交响
- **蜀绣《一针入画》**：成都蜀绣，丝线里藏着的时光
- **木版年画《门神醒来》**：杨柳青年画，旧纸上的新故事

每个案例都有完整的 8 模块内容和 8 个分镜镜头，可以直接复制提示词使用。

## 接下来做什么

V2.2.0 计划接入 Seedream 图像生成和 Seedance 视频生成，实现真正的端到端 AI 非遗短片创作。从文字到画面到视频，全链路 AI 驱动。

还会扩展到 20+ 非遗类型（皮影戏、景泰蓝、苏绣、古琴……），并增加项目分享和多人协作功能。

## 试试看

- 快速体验：下载 HTML 版，双击即用
- AI 真实生成：下载源码，配好 .env，`npm run dev:all`
- 在线体验：https://shops-solomon-browsing-orange.trycloudflare.com（评审直接打开，无需配置，AI 真实生成）

---

非遗不该只活在博物馆里。让 AI 帮你把它变成一支短片。
